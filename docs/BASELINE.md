# Цифры «до» — точка отсчёта работ

Снято 07.09.2026 в начале работ (ветка `redesign`, коммит `2598fb9`).
С этими значениями сверяется Волна 9. Если что-то стало хуже — это регрессия,
а не «наверное, так и должно быть».

## Сборка клиента

| | CRA (react-scripts 5) | Vite 7 | |
|---|---|---|---|
| главный кусок JS | 768 154 Б | 674 640 Б | −12% |
| стили | 226 990 Б | 197 600 Б | −13% |
| чанк панели управления | 98 493 Б | 63 240 Б | −36% |
| время сборки | ~60 с | 2,5 с | |
| пакетов в дереве | 1346 | 287 | −79% |
| уязвимостей npm audit | много | 0 | |

Стили пока **одним файлом**: `theme.css` (4896 строк) импортируется в точке
входа, поэтому делить нечего. Разделение — Волна 3, вместе с переездом
структуры.

## Прод, живой трафик (замерено 07.09.2026 до начала работ)

Сжатие включено отдельным файлом `/etc/nginx/conf.d/gzip.conf`:

| | до сжатия | после |
|---|---|---|
| `/api/emoji/catalog` | 1 325 237 Б | 140 380 Б (−89%) |
| `main.js` | 795 008 Б | 219 648 Б (−73%) |
| `main.css` | 226 990 Б | 38 133 Б (−84%) |

Холодная загрузка приложения: ~2,3 МБ → ~400 КБ.

Каталог смайликов запрашивается **условно** и почти всегда отдаёт `304` с
нулевым телом — полный ответ платится при первой загрузке или реальной смене
каталога.

## Холодный старт: что дёргается

17 уникальных обращений на старте (без учёта картинок):

```
/users/me            /users             /contacts          /groups
/favorites           /unread            /comments          /session/version
/emoji/catalog       /stickers/catalog  /notification-settings
/messages/meta/last  /messages/meta/recent  /messages/threads
```

В dev-режиме каждое видно дважды — это `React.StrictMode`, а не дубль в коде.

## Код

| Зона | Объём |
|---|---|
| `client/src` боевой код | 24 571 строка TS/TSX |
| `client/src` стили | 5 314 строк CSS (`theme.css` — 4 896) |
| `server` боевой код | 13 263 строки JS, 68 файлов |
| `desktop/src` | 1 377 строк |
| **Итого боевого кода** | **~44 500 строк** |

Точки концентрации: `Chat.tsx` 3678, `ChatWindow.tsx` 1647,
`SuperAdminApp.tsx` 1557, `server/index.js` 1358, `server/db.js` 1347.

## Тесты

| | было | стало (Волна 0) |
|---|---|---|
| клиент | 33 файла, 179 тестов | 36 файлов, **206** |
| сервер | 12 файлов, 120 тестов | 12 файлов, 120 |
| `Chat.tsx` | **0** | **27** |

## Контракт клиент–сервер

143 REST-маршрута; 109 вызовов из клиента, **типизировано 5**.
Это цель Волны 3.

## Документация

`CLAUDE.md` — 230 КБ, **около 70 000 токенов в каждой сессии**.
Цель Волны 2 — не больше 8 000.
