# NBRT_MirasChatServer

Монорепозиторий MirasChat: `client` (веб, React), `server` (Node/Express + Socket.io + SQLite), `desktop` (Electron-обёртка клиента под Windows), `mobile` (Capacitor).

## Обновление продакшн-сервера после `git pull`

Продакшн крутится на `cagrizzz.ru` (пользователь `gri`), путь до репозитория — `~/projects/miraschat`. Веб-клиент собирается на самом сервере и раздаётся статикой из `/var/www/miraschat/dist` (владелец `www-data`), бэкенд — процесс `MirasChatServer` в pm2.

### 0. Подтянуть код

```bash
cd ~/projects/miraschat
git pull origin main --ff-only
```

### 1. Клиент (веб) — если менялось что-то в `client/`

```bash
cd ~/projects/miraschat/client
npm install                         # только если менялся package.json/lock
npm run build                       # соберёт в client/build; заодно прописывает
                                     # версию (git-хэш + время сборки) в
                                     # src/version.ts — см. "Настройки" в приложении
sudo rsync -a --delete /home/gri/projects/miraschat/client/build/ /var/www/miraschat/dist/
sudo chown -R www-data:www-data /var/www/miraschat/dist/
```

`rsync`/`chown` разрешены пользователю `gri` без пароля (см. `sudo -l` — правило добавлено в `/etc/sudoers` через `visudo` для именно этих двух команд), так что весь блок можно скопировать одной строкой:

```bash
cd ~/projects/miraschat/client && npm run build && \
  sudo rsync -a --delete /home/gri/projects/miraschat/client/build/ /var/www/miraschat/dist/ && \
  sudo chown -R www-data:www-data /var/www/miraschat/dist/
```

Перезапуск pm2 для чисто клиентских изменений **не нужен** — это статика, отдаёт её nginx.

### 2. Сервер (бэкенд) — если менялось что-то в `server/`

```bash
cd ~/projects/miraschat/server
npm install                         # только если менялся package.json/lock
pm2 restart MirasChatServer
```

Это разорвёт активные сокет-соединения (пользователи увидят кратковременный дисконнект чата), так что без необходимости не перезапускать.

### 3. Проверить, что раскатилось

```bash
pm2 status MirasChatServer
curl -s -o /dev/null -w '%{http_code}\n' https://cagrizzz.ru/miraschat/
```

В приложении: Настройки → внизу списка строка вида `MirasChat <хэш> · <дата сборки>` — по ней видно, точно ли раскатился актуальный коммит. Хэш с `+` на конце означает, что сборка была сделана при незакоммиченных изменениях в рабочей копии на сервере.

## Сборка Android-приложения (.apk)

Android-клиент — это тот же веб-клиент, упакованный в Capacitor: `mobile/www` содержит собранную статику React, Android-проект лежит в `mobile/android`. Нативного кода в приложении нет, кроме двух плагинов Capacitor (`@capacitor/app` — кнопка «назад» и сворачивание, `@capacitor/local-notifications` — системные уведомления).

Важно: приложение **не** ходит в локальный сервер. В сборку зашиваются адреса продакшна (`https://cagrizzz.ru/miraschat/api`), см. шаг 3 — как их поменять.

### 0. Что должно быть установлено

| Что | Версия | Как проверить |
| --- | --- | --- |
| JDK | **17** — Gradle 8.2.1 не запускается на JDK 21+ | `java -version` |
| Android SDK | Platform **34** + Build-Tools | наличие `<sdk>/platforms/android-34` |
| Node.js | 18+ | `node -v` |

На текущей машине сборки: JDK 17 (Eclipse Adoptium), SDK в `C:\Android\sdk`. Путь до SDK берётся из `mobile/android/local.properties` (файл в `.gitignore`, на каждой машине свой):

```
sdk.dir=C\:\\Android\\sdk
```

Если файла нет — создайте его вручную (обратные слэши экранируются, как в примере) или один раз откройте проект в Android Studio, она пропишет путь сама. `JAVA_HOME` должен указывать на JDK 17; Gradle и Android SDK ставятся вместе с Android Studio, отдельный Gradle не нужен — используется wrapper (`gradlew`), он сам скачает нужную версию при первом запуске.

Зависимости `mobile/` ставятся один раз:

```bash
cd mobile
npm install
```

### 1. Поднять версию

Версия приложения живёт в `mobile/android/app/build.gradle` и **не меняется автоматически**:

```gradle
versionCode 3
versionName "1.2"
```

- `versionName` — то, что видит пользователь (и что идёт в имя файла APK).
- `versionCode` — целое число, которое **обязано расти**. Android откажется устанавливать сборку с `versionCode` меньше уже установленной.

Поднимайте оба перед каждой раздаваемой сборкой.

### 2. Собрать веб-клиент и синхронизировать с Android-проектом

```bash
cd mobile
npm run prepare-app
```

Одна команда делает два шага (можно запускать и по отдельности: `npm run build:client`, `npm run sync`):

1. `scripts/build-client.js` собирает `client/` в режиме production в `client/build-mobile` (отдельно от обычного `client/build`, чтобы не перетирать веб-сборку) с `PUBLIC_URL=.` — внутри APK нет веб-сервера, пути должны быть относительными — и копирует результат в `mobile/www`.
2. `npx cap sync android` копирует `mobile/www` в `android/app/src/main/assets/public` и обновляет список плагинов Capacitor.

Шаг обязателен после **любой** правки в `client/` — Gradle сам клиент не пересобирает.

### 3. Куда будет ходить приложение

По умолчанию в продакшн. Переопределяется переменными окружения на время сборки:

```bash
MIRASCHAT_API_BASE_URL=https://cagrizzz.ru/miraschat/api \
MIRASCHAT_SOCKET_URL=https://cagrizzz.ru \
MIRASCHAT_SOCKET_PATH=/miraschat/socket.io \
npm run prepare-app
```

На Windows в PowerShell:

```powershell
$env:MIRASCHAT_API_BASE_URL='http://192.168.24.2:3010/api'; npm run prepare-app
```

Адреса печатаются в начале сборки — сверьтесь с выводом. Для сборки под локальный сервер укажите IP машины в сети (не `localhost`: для телефона это он сам).

### 4. Собрать APK

```bash
cd mobile/android
./gradlew assembleDebug
```

На Windows в PowerShell — `.\gradlew.bat assembleDebug`. Первый запуск скачивает Gradle и зависимости (несколько минут), последующие — около минуты.

Результат: `mobile/android/app/build/outputs/apk/debug/app-debug.apk`.

Готовый файл кладём в `mobile/release` под версионным именем (папка в `.gitignore`, APK в репозиторий не коммитятся):

```bash
cp app/build/outputs/apk/debug/app-debug.apk ../release/MirasChat-1.2-debug.apk
```

### 5. Проверить, что собралось именно то

```bash
# путь до SDK — тот же, что в local.properties (ANDROID_HOME на машинах сборки не задан)
C:/Android/sdk/build-tools/34.0.0/aapt2 dump badging ../release/MirasChat-1.2-debug.apk | head -8
```

Должно быть видно `versionCode`/`versionName` из шага 1, `package: name='ru.miras.mirasChat'` и разрешение `android.permission.POST_NOTIFICATIONS` (оно приезжает из плагина уведомлений через manifest merger, в нашем `AndroidManifest.xml` его нет и добавлять не нужно).

### 6. Поставить на телефон

По кабелю, с включённой отладкой по USB:

```bash
adb install -r mobile/release/MirasChat-1.2-debug.apk
```

Либо просто скинуть `.apk` на устройство и открыть — Android спросит разрешение на установку из неизвестного источника.

При первом запуске приложение запросит разрешение на уведомления (Android 13+). Без него системные уведомления не появятся — внутренние всплывающие в самом приложении работать будут.

### 7. Выложить обновление для тех, у кого приложение уже стоит

Android **не даёт приложениям ставить пакеты молча** — тихого обновления, как на десктопе, здесь не будет ни при каких настройках. Максимум, что можно: узнать о новой версии и открыть ссылку на APK, дальше скачивание и установку ведёт сама система. Поэтому в мобильных Настройках кнопка «Обновить» обязательна, в отличие от десктопа, где кнопок нет.

Приложение при открытии Настроек читает `https://cagrizzz.ru/miraschat/updates/android.json` и сравнивает `versionCode` оттуда с установленным. Сравнение именно по коду, а не по имени версии: имя — подпись для человека, а отказывается ставить старое поверх нового Android именно по коду.

Манифест собирается из `build.gradle`, чтобы номера в нём не разъезжались с тем, что реально лежит рядом:

```bash
cd mobile
npm run update-manifest
```

Он ждёт APK в `mobile/release/` под именем `MirasChat-<versionName>-debug.apk` и кладёт туда же `android.json`. Заливать оба файла:

```bash
scp "mobile/release/MirasChat-X.Y.Z-debug.apk" "mobile/release/android.json" mirachat-prod:/var/www/miraschat/updates/
```

Про подпись здесь важнее, чем обычно: обновление встанет поверх установленного, только если собрано **тем же debug-ключом**. С другой машины Android потребует сначала удалить приложение — вместе с сессией. Подробности ниже.

### Про подпись: debug против release

`assembleDebug` подписывает APK автоматически сгенерированным debug-ключом (`~/.android/debug.keystore`). Для раздачи внутри организации этого достаточно, но помните:

- debug-ключ **свой на каждой машине разработчика**. APK, собранный на другом компьютере, не встанет поверх уже установленного — Android увидит другую подпись и потребует сначала удалить приложение (данные и сессия при этом теряются). Собирайте релизы для раздачи всегда с одной машины.
- в Google Play такой APK не примут.

Если понадобится настоящая release-сборка (`assembleRelease`), нужно завести keystore и добавить `signingConfigs` в `mobile/android/app/build.gradle`. Сейчас этого нет — `assembleRelease` соберёт неподписанный APK, который не установится.

### Иконки и splash-экран

Пересобираются из `desktop/assets/icon.svg` и `mobile/assets/icon-foreground.svg`, только если менялся логотип:

```bash
cd mobile
node scripts/gen-icons.js     # launcher + adaptive icon во все mipmap-*
node scripts/gen-splash.js    # splash.png во все drawable-*
```

После этого — обычная сборка (шаги 2 и 4).

### Если что-то не собирается

| Симптом | Причина |
| --- | --- |
| `SDK location not found` | нет `mobile/android/local.properties` или неверный `sdk.dir` |
| `Unsupported class file major version` | Gradle запустился не на JDK 17 — проверьте `JAVA_HOME` |
| Приложение открылось, но экран белый | не выполнен `npm run prepare-app`, в `assets/public` пусто или лежит старая сборка |
| В приложении старый интерфейс | то же самое: Gradle пересобрал только обёртку, забыли пересобрать клиент |
| Не приходят уведомления | разрешение на уведомления не выдано, либо у приложения включена экономия батареи |

## Пуш-уведомления (FCM)

Пока приложение на экране, уведомления показывает сам клиент по событию сокета. Как только Android сворачивает приложение, сокет умирает: Chromium душит таймеры фонового рендерера, потом добавляются Doze и убийство процесса под память. Единственный канал, который это переживает, — пуш через Firebase Cloud Messaging: он приходит в сервисы Google Play и будит приложение, в том числе выгруженное.

Проект Firebase — `cg-chat-7b4bc`, тариф Spark (бесплатный, FCM в нём без ограничений).

### Что где лежит

| Файл | Назначение | В репозитории |
| --- | --- | --- |
| `mobile/android/app/google-services.json` | конфиг клиента, попадает внутрь APK | да |
| `server/fcm-service-account.json` | ключ на отправку от имени проекта | **нет**, в `.gitignore` |

`google-services.json` секретом не является — он и так распакован внутри каждого APK, а ключ в нём привязан к имени пакета. Держим в репозитории, чтобы сборка заводилась из чистого клона. Ключ сервисного аккаунта — наоборот, полноценный доступ на рассылку; путь до него можно переопределить переменной `FCM_SERVICE_ACCOUNT` в `server/.env`.

Без ключа сервер поднимается и работает как раньше, просто без пушей — в лог пишется `[push] ключ сервисного аккаунта не найден`. Это сделано намеренно: забытый файл на проде не должен ронять мессенджер целиком ради функции, которая дублирует сокет.

### Как это работает

Сервер шлёт пуш только тем получателям, у кого нет живого сокета (см. `services/push.js` и врезку в обработчике `chat_message`). Пока сокет жив, уведомление рисует сам клиент — иначе на одно сообщение получилось бы две карточки.

**Текст сообщения в пуш не кладётся.** Уходят только имя отправителя, `chatId` и `messageId`; сам текст человек видит уже в приложении, которое забирает его с нашего сервера. Так содержимое внутренней переписки не проходит через инфраструктуру Google. Если когда-нибудь понадобится предпросмотр в шторке, как в Telegram, — это поле `body` в `services/push.js`, но решение осознанное.

Пуш отправляется блоком `notification`, а не только `data`. Data-only сообщение показать некому, когда приложение выгружено: плагин Capacitor умеет обработать его лишь при живом JS-мосте, иначе оно просто ложится в `lastMessage` до следующего запуска. Блок `notification` рисует система, без участия нашего кода.

Токены устройств живут в таблице `device_tokens`, `UNIQUE` по самому токену: он принадлежит установке приложения, а не человеку, и при смене владельца телефона строка должна переезжать. Клиент регистрирует токен при каждом запуске (Firebase переиздаёт его сам после переустановки или очистки данных) и снимает при выходе из аккаунта. Протухшие токены сервер удаляет сам, когда FCM отвечает по ним ошибкой.

### Раскатка на прод

Сверх обычного обновления нужно:

```bash
cd ~/projects/miraschat/server
npm install                         # появился firebase-admin
# положить ключ сервисного аккаунта в server/fcm-service-account.json
pm2 restart MirasChatServer
```

Проверить, что подхватилось: `pm2 logs MirasChatServer` — должна быть строка `[push] FCM подключён, проект cg-chat-7b4bc`.

### Если пуши не приходят

| Симптом | Причина |
| --- | --- |
| В логе `ключ сервисного аккаунта не найден` | нет `server/fcm-service-account.json` или неверный `FCM_SERVICE_ACCOUNT` |
| В консоли приложения `Ошибка регистрации в FCM` | на устройстве нет сервисов Google, либо `google-services.json` не от того проекта |
| Приходят, только пока приложение открыто | сокет ещё жив — это штатно, пуш шлётся только офлайн-получателям |
| Не приходят вообще на конкретном телефоне | не выдано разрешение на уведомления, либо приложение в списке жёсткой экономии батареи (MIUI, EMUI) |

## Автообновление десктоп-клиента

Обновления раздаются обычной статикой с нашего же сервера — `electron-updater` с провайдером `generic`, адрес прописан в `desktop/package.json` (`build.publish`). Никакого отдельного сервиса не нужно, только каталог с файлами и nginx.

Приложение проверяет наличие новой версии через 15 секунд после запуска, дальше раз в четыре часа, и ещё раз при каждом открытии Настроек. Дальше всё происходит само, без единого нажатия: это замена той же самой программы, а не установка новой.

Найдя версию новее, клиент скачивает её фоном. Момент установки выбирается по состоянию окна:

| Окно | Что происходит |
| --- | --- |
| Свёрнуто в трей и не в фокусе | ставим сразу после докачки, приложение перезапускается само и возвращается в трей уже обновлённым |
| Открыто | ждём, обновление встанет при закрытии приложения (`autoInstallOnAppQuit`) |

Ветка с треем здесь принципиальна: закрытие окна прячет приложение, а не завершает его, поэтому выхода можно ждать неделями. Без неё обновление висело бы скачанным и неустановленным до перезагрузки машины.

В Настройках всё это отражается строками «Загрузка обновления» и «Обновление X встанет при закрытии». Кнопка там одна и необязательная — «Перезапустить», для тех, кто не хочет ждать. В подвале Настроек вместо хэша сборки показан номер версии — иначе сообщение о доступном обновлении не с чем сравнить. Хэш остаётся в вебе, где номера версии просто нет.

### Расписание установки

Супер-админ может назначить момент, раньше которого клиенты не ставят обновление: в панели управления, карточка «Обновления». Хранится в `app_settings` (ключ `update_not_before`) в unix-миллисекундах, задаётся и показывается по Москве.

Откладывается **только установка**. Скачивание идёт сразу, как обычно: иначе в назначенный час все клиенты разом пойдут на сервер за 80 МБ.

Когда обновление скачано, клиент решает так:

| Условие | Что происходит |
| --- | --- |
| Время не задано | ставим по обычным правилам (таблица выше) |
| Время раньше, чем `releaseDate` из `latest.yml` | то же самое — расписание осталось от прошлого выпуска |
| Время ещё не наступило | обновление лежит скачанным; в Настройках строка «Обновление X установится ДД.ММ.ГГГГ, ЧЧ:ММ» |
| Время наступило, приложение было выключено | ставим сразу при запуске, не дожидаясь, пока приложение свернут |
| Время наступило, приложение работает | по обычным правилам |

Сравнение с `releaseDate` — обязательное. Без него однажды выставленная дата навсегда отпускала бы все будущие версии сразу: любое прошедшее время считалось бы «уже можно».

«Приложение было выключено» определяется по окну в пять минут от запуска. Строгого признака тут нет, а окно нужно, чтобы медленная закачка не выдернула приложение у человека, который уже успел сесть работать.

Расписание клиент читает с `GET /api/updates/schedule` — без авторизации. У main-процесса Electron нет сессии (токен живёт в рендерере), а в ответе одна метка времени. Адрес выводится из `build.publish`, чтобы хост не был прописан в двух местах.

Кнопки «Перезапустить» в состоянии «установится в ЧЧ:ММ» нет намеренно: она обошла бы назначенный час, ради которого расписание и заводили.

**Расписание живёт в клиенте**, поэтому действует начиная с той версии, где оно есть. На версиях старше назначенное время ни на что не влияет — они ставят обновление сразу.

### Настройка nginx (разово)

Файлы обновлений лежат в `/var/www/miraschat/updates/`, **рядом** с `dist/`, а не внутри него: `dist/` целиком перезаписывается `rsync --delete` при каждой выкладке клиента, и установщик оттуда исчезал бы после первого же деплоя.

Из-за этого нужен отдельный `location`. Без него запрос уходит в блок `location /miraschat/`, где `alias` ведёт в `dist/`, файл не находится и срабатывает SPA-фолбэк `try_files ... /miraschat/index.html`. Внешне всё выглядит рабочим — HTTP 200 на любой URL, — но `electron-updater` получает HTML вместо YAML и обновлений не видит.

В `/etc/nginx/sites-available/cagrizzz.ru`, перед блоком `location /miraschat/`:

```nginx
    location /miraschat/updates/ {
        alias /var/www/miraschat/updates/;
        default_type application/octet-stream;
        try_files $uri =404;
        add_header Access-Control-Allow-Origin *;
    }
```

`try_files $uri =404` здесь обязателен: он и отменяет фолбэк на `index.html`. `Access-Control-Allow-Origin` — тоже: `android.json` читает `fetch()` изнутри Capacitor-приложения, у которого origin не `https://cagrizzz.ru` (обычно `https://localhost`), и без этого заголовка браузер внутри WebView молча блокирует ответ — `fetch` падает с сетевой ошибкой, которую код проверки обновлений тихо проглатывает. Desktop-клиента это не касается: `electron-updater` не браузер и CORS не проверяет, но заголовок безопасно распространяется на все файлы в этом `location` — раздача публичная, секретов тут нет. Применить:

```bash
sudo nginx -t && sudo systemctl reload nginx
```

Сам каталог создаётся один раз: `sudo mkdir -p /var/www/miraschat/updates && sudo chown gri /var/www/miraschat/updates`.

### Выпуск новой версии

**1.** Поднять `"version"` в `desktop/package.json`. Автоматически она не меняется, а обновление завязано именно на неё: забыли поднять — клиенты просто не увидят новую сборку, молча.

**2.** Собрать:

```bash
cd desktop
npm run dist:win
```

**3.** В `desktop/release/` появятся три нужных файла:

| Файл | Зачем |
| --- | --- |
| `MirasChat Setup X.Y.Z.exe` | сам установщик |
| `latest.yml` | манифест: версия, размер, хэш — по нему клиент понимает, что вышло обновление |
| `MirasChat Setup X.Y.Z.exe.blockmap` | карта блоков для докачки только изменившихся кусков вместо всех 80 МБ |

Заливать нужно **все три**. Без `latest.yml` клиент не узнает об обновлении; без `.blockmap` обновление скачается целиком.

**4.** Положить их на сервер:

```bash
scp "desktop/release/MirasChat Setup X.Y.Z.exe" "desktop/release/latest.yml" "desktop/release/MirasChat Setup X.Y.Z.exe.blockmap" mirachat-prod:/var/www/miraschat/updates/
```

Старые установщики можно не удалять — клиенты ориентируются на `latest.yml`, а старые файлы пригодятся, если понадобится откатить кого-то вручную.

**5.** Проверить, что раздаётся:

```bash
curl -s https://cagrizzz.ru/miraschat/updates/latest.yml
```

Должен вернуться YAML с номером версии, а не HTML страницы приложения. Пришёл HTML — значит, отсутствует блок `location /miraschat/updates/` из раздела «Настройка nginx» выше, и запрос ушёл в SPA-фолбэк. Проверять стоит именно тело ответа: код в обоих случаях 200, по нему подмену не видно.

### Про подпись

Установщик не подписан сертификатом. На работу автообновления это не влияет — `electron-updater` без настроенной подписи её и не проверяет, целостность файла контролируется хэшем из `latest.yml`. Но SmartScreen будет предупреждать при установке каждой новой версии, как предупреждает сейчас при первой. Сертификат для подписи кода стоит порядка 200–400 долларов в год; для внутренней раздачи обычно живут без него.

### Автозагрузка

После установки автозагрузка включается сама, при первом запуске приложения. Отметку о том, что мы уже вмешивались, приложение хранит в `app-state.json` в своей папке пользовательских данных — поэтому выключенная вручную автозагрузка обратно не включается. В dev-режиме (`npm start`) не срабатывает вовсе, иначе в автозагрузку рабочей машины попал бы путь до `electron.exe` из `node_modules`.

Ярлыки на рабочем столе и в меню «Пуск» создаёт установщик. **Закрепить приложение на панели задач программно нельзя** — Windows намеренно закрыла этот API, обходные пути через реестр ломаются обновлениями и ловятся антивирусами. Ни один установщик этого не умеет, закрепление остаётся действием пользователя.

## Календарь

Раздел «Календарь» — это тонкая обёртка над виджетом `client/src/calendar/`. Так сделано не ради красоты: тот же календарь предстоит показывать внутри пространств, и всё, что отличает один календарь от другого, собрано в одном объекте `scope`.

```tsx
<CalendarWidget scope={{ kind: 'personal' }} />        // раздел «Календарь»
<CalendarWidget scope={{ kind: 'space', id: 7 }} />    // карточка пространства, когда появится
```

Ни одно представление не знает, чей календарь оно рисует. `scope_kind`/`scope_id` заложены и в таблицу `calendar_events`, и в API — добавлять разделение задним числом пришлось бы вместе с миграцией уже накопленных событий.

### Из чего собран

| Файл | Отвечает за |
| --- | --- |
| `dates.ts` | арифметика по календарным дням в московской зоне |
| `types.ts` | `scope`, вхождения, черновик события |
| `api.ts` | запросы к `/api/calendar` |
| `useCalendarData.ts` | загрузка диапазона под текущий режим, слои |
| `layout.ts` | раскладка пересекающихся событий в сетке времени |
| `MonthView` · `TimeGridView` · `AgendaView` · `MiniMonth` | представления; `MiniMonth` самостоятелен и годится как отдельный виджет |
| `EventDialog` | создание и правка |
| `CalendarWidget` | шапка, боковая панель, склейка всего перечисленного |

### Время

Всё считается в московской зоне независимо от зоны устройства — как и переписка. Для календаря это важнее: «встреча во вторник 14:00» должна означать один и тот же вторник у всех, иначе у человека в другой зоне событие уедет в соседнюю клетку сетки.

Основная валюта модуля — не момент, а календарный день строкой `'YYYY-MM-DD'`. Арифметика по таким строкам не зависит ни от смещения зоны, ни от длины суток; моменты появляются только на границах — при запросе к серверу и при отрисовке времени.

### Повторы

Разворачиваются **на сервере** (`services/calendarEvents.js`), клиент получает готовые вхождения и о правилах повторения не знает вовсе. Благодаря этому одна и та же выборка одинаково обслуживает любой scope и любой будущий виджет.

Поддержаны `daily`/`weekly`/`monthly`/`yearly` с интервалом и необязательной датой окончания. Несуществующие даты пропускаются, а серия продолжается: 31 числа ежемесячно даёт январь, март, май — но не февраль; 29 февраля ежегодно попадает только в високосные годы.

Две ловушки, на которые уже наступили и которые закрыты тестом:

- `Number(null) === 0`, поэтому «без даты окончания» нужно проверять явно — иначе бесконечная серия обрезается 1970 годом и не показывается вообще;
- назначенное время сравнивается с датой сборки, а несуществующие дни не должны обрывать цикл — иначе серия молча заканчивается на первом коротком месяце.

### Задачи

Задача отличается от события только тем, что её можно выполнить. Отметка лежит в отдельной таблице `calendar_task_completions` и привязана к **вхождению**, а не к событию: у повторяющейся задачи «сдать отчёт каждый понедельник» галочка закрывает один понедельник, а не всю серию.

Правка времени или правила повтора сбрасывает отметки — они привязаны к моментам, которых после правки больше не существует.

### Дни рождения

Отдельный слой, который можно выключить. Берутся из `birth_date` контактов и **не копируются** в таблицу событий: это представление поля профиля, а не события. Скопируй мы их — пришлось бы поддерживать копии актуальными при каждой правке даты рождения.

### API

| Метод | Назначение |
| --- | --- |
| `GET /api/calendar/events?from&to&scope_kind&scope_id` | развёрнутые вхождения диапазона + дни рождения |
| `POST /api/calendar/events` | создать |
| `PUT /api/calendar/events/:id` | изменить (только владелец) |
| `DELETE /api/calendar/events/:id` | удалить серию целиком |
| `POST /api/calendar/events/:id/complete` | отметить вхождение выполненным |
| `POST /api/calendar/events/:id/response` | ответ участника на приглашение |

Диапазон ограничен 400 днями: сетке месяца нужно шесть недель, году — двенадцать месяцев, всё большее — либо ошибка клиента, либо попытка вытащить базу одним запросом.

### Листание

Колесом на компьютере и вертикальным свайпом на телефоне — плюс стрелки на клавиатуре. Правило одно (`gestures.ts`), а шаг у каждого представления свой: месяц листает месяцы, неделя — недели, день — дни.

Три вещи, без которых жест ощущается сломанным:

- **Порог накопления.** Тачпад шлёт десятки мелких событий на одно движение пальцем; без накопления до 120px календарь пролистывал бы полгода за раз.
- **Пауза после шага.** Инерция тачпада продолжает слать события уже после того, как человек убрал руку. Проверено: 30 событий подряд дают ровно один шаг, а не пятнадцать.
- **Уступать внутренней прокрутке.** В сетке недели и дня жест сначала принадлежит суткам: листание начинается, только когда сутки долистаны до края. Иначе до вечерних часов нельзя было бы добраться вовсе.

Смена направления обнуляет накопленное, чтобы «вниз-вниз-вверх» не складывалось в шаг вниз. В «Расписании» жест выключен: там длинный список, и прокручивать его — это прокрутка, а не переход.

Горячие клавиши: стрелки — листать, `T` — сегодня, `M`/`W`/`D`/`A` — представления. Кириллические раскладки (`Е`, `Ь`, `Ц`, `В`, `Ф`) обрабатываются наравне с латинскими: переключать раскладку ради горячей клавиши никто не станет. В полях ввода клавиши принадлежат тексту.

### Чего пока нет

Перетаскивания событий мышью. В Google Calendar это заметная часть работы с сеткой, но она требует отдельного слоя обработки указателя поверх раскладки; создание и правка сейчас идут через диалог.

## Кто на какой версии

В панели управления, в таблице пользователей, колонка «Версия» показывает, до кого обновление доехало.

Клиент сам отчитывается при появлении сессии (`utils/reportVersion.ts` → `POST /api/session/version`). Версия берётся оттуда же, откуда её видит человек: у десктопа `app.getVersion()`, у Android `App.getInfo()`, у веба — хэш сборки, своего номера версии там нет.

Строки хранятся **по платформам** (`user_app_versions`, уникальность по паре пользователь+платформа), а не колонкой в `users`: один и тот же человек сидит с компьютера и с телефона, и единственная колонка показывала бы ту версию, с которой он заходил последним, — то есть ровно не то, что нужно при раскатке.

Пусто — панель показывает **`old`**. Это значит одно из двух: сборка старше появления этой телеметрии либо человек с тех пор не заходил. Различать их незачем — в обоих случаях обновление до него не доехало.

Наведение на версию показывает, когда клиент отчитывался последний раз.

## Версионирование

- **Веб-клиент** (`client/`): версию не нужно вручную трогать. `client/scripts/generate-version.js` запускается автоматически перед `npm start`/`npm run build` (хуки `prestart`/`prebuild` в `client/package.json`) и штампует короткий git-хэш + время сборки в `client/src/version.ts` (в `.gitignore`, не коммитится). Именно эта версия видна в UI (см. выше).
- **Десктоп-приложение** (`desktop/`): версия — это поле `"version"` в `desktop/package.json`, её нужно поднимать **вручную** перед каждой сборкой инсталлятора (`npm run dist:win`), она попадает в имя файла (`MirasChat Setup X.Y.Z.exe`) и в метаданные Electron-приложения. Автоматически не меняется — не забывайте бампить перед `dist:win`.
- **Android-приложение** (`mobile/`): `versionCode` и `versionName` в `mobile/android/app/build.gradle`, тоже **вручную** перед каждой сборкой APK — подробности в разделе «Сборка Android-приложения». Внутри самого приложения (Настройки → низ списка) показывается git-версия веб-клиента, как и в вебе, — она к `versionName` отношения не имеет.

## Быстрый доступ

Алиас `mirachat-prod` в `~/.ssh/config` (только на машине разработки, не на самом сервере) указывает на `gri@cagrizzz.ru` с ключом `~/.ssh/mirachat_deploy` — им пользуется Claude Code для деплоя по запросу «обнови сервер».
