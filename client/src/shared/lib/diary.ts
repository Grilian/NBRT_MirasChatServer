/**
 * Название личного хранилища записей.
 *
 * Было настройкой организации («Избранное», «Облако», «Архив»), пока смысл
 * чата был размытым — «место, куда складывают». Складывание чужого целиком
 * ушло к Следам, осталось одно значение: собственные записи человека. Выбирать
 * стало нечего, и слово переехало в константу — серверный близнец лежит в
 * `services/chatParticipants.js` (DIARY_NAME).
 */
export const DIARY_NAME = 'Дневник';

/**
 * Как называется вся поверхность целиком — строка в списке чатов.
 *
 * Внутри она переключается между Следами и Дневником, но снаружи это одна
 * вещь, и называется она по главному в ней: Следы.
 */
export const TRACES_NAME = 'Следы';
